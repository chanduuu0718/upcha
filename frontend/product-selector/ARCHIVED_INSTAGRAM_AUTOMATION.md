# Archived direction

The project no longer uses Instagram automatic replies as the primary product-delivery workflow. The public catalog under `frontend/catalog` is the customer-facing product destination.

Instagram webhook code/config may remain temporarily for reference, but it is not part of the main product flow.
